<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

  <TextView
      android:id="@+id/text1"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:text="Select a Body Part here :"
      android:textSize="15dp"
      android:textStyle="bold"
      android:layout_margin="20dp"/>

        <Spinner
            android:id="@+id/workoutspinner"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:entries="@array/workoutlist"
            />
    </LinearLayout>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/selectbutton"
        android:text="See the workout list"
        android:textStyle="bold"
        android:layout_margin="40dp"
        android:onClick="onClickFindWorkouts"
        />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/listtextID"
        android:text="Your workouts..."
        android:textColor="#C42929"
        android:layout_gravity="center"
        android:textStyle="bold"
        android:textSize="25dp"/>

</LinearLayout>